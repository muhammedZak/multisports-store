export function ShellFooter() {
  return (
    <footer className='mt-auto border-t border-[var(--color-border)] bg-white'>
      <div className='ds-container flex flex-col gap-2 py-8 text-sm sm:flex-row sm:items-center sm:justify-between'>
        <p className='mb-0 font-semibold text-[var(--color-ink)]'>MultiSport</p>

        <p className='mb-0 text-[var(--color-muted)]'>Gear for every game.</p>
      </div>
    </footer>
  );
}
